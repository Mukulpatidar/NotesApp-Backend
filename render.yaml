services:
  - type: web
    name: notesapp-backend
    env: docker
    plan: free

    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/notesapp-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
